server:
  port: 8082

spring:
  application:
    name: farmer-service

  datasource:
    url: jdbc:mysql://localhost:3306/farmer_db?createDatabaseIfNotExist=true
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest