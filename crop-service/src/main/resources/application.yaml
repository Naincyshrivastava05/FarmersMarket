server:
  port: 8084

spring:
  application:
    name: crop-service

  datasource:
    url: jdbc:mysql://localhost:3306/crop_db?createDatabaseIfNotExist=true
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest